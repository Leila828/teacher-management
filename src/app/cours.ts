export class Cours {
  // tslint:disable-next-line:variable-name
          _id: string;
          title: string;
          caption: string;
          url: string;
          createdAt: Date;
  // tslint:disable-next-line:variable-name
          _teacherId: string;
}
